<li>
	{{burger_name}}

	<button class="change-burger" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}Devoured!{{else}}Undevoured!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Delete Burger!</button>
</li>
